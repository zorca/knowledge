import { createStyles } from "@material-ui/core/styles";

export default theme => {

    return createStyles({
        root: {
            borderRadius: "10px",
        },
        contained: {
            borderRadius: "10px",
        }
    });
};
